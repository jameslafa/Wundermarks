<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://fonts.googleapis.com/css?family=Ubuntu:400,400i,500,700" rel="stylesheet">
    <title>Wundermarks</title>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>
  <body class="popup <%= "c-#{params[:controller].parameterize} a-#{params[:action].parameterize}" %>">
    <nav class="navbar navbar-default env-<%= Rails.env %>">
      <div class="container-fluid">
        <div class="navbar-header">
          <span class="navbar-brand"><%= t('header.navigation.brand') %></span>
        </div>        
      </div> <!-- .container-fluid -->
    </nav>

    <div class="container-fluid">
      <%= render "shared/flash" %>
      <%= yield %>
    </div>
  </body>
</html>
