<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Wundermarks</title>
    <link rel="icon" type="image/png" href="<%= image_url("apple-touch-icon.png") %>">
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>
  <body class="devise <%= "c-#{params[:controller].parameterize} a-#{params[:action].parameterize}" %>">
    <div class="container-fluid">
      <div class="row devise-navbar">
        <div class="col-md-12">
          <span class="logo">W</span>
          <% if params[:controller] == 'devise/sessions' && params[:action] == 'new' %>
          <span class="action"><%= t("devise.sessions.new.title") %></span>
          <% elsif params[:controller] == 'registrations' && params[:action] == 'new' %>
          <span class="action"><%= t("devise.registrations.new.title") %></span>
          <% elsif params[:controller] == 'devise/passwords' && params[:action] == 'new' %>
          <span class="action"><%= t("devise.passwords.new.title") %></span>
          <% end %>

        </div>
      </div>
      <%= render "devise/shared/flash" %>
      <%= yield %>
    </div>
  </body>
</html>
